# Deploy to VPS — DISABLED
# Fortress runs on WSL (Windows), not a VPS. This workflow is kept for
# reference only. The old Hostinger VPS was decommissioned 2026-05-08.
# Deploy manually from WSL: git pull && sudo systemctl restart fortress-dashboard-v4

name: Deploy to VPS

on:
  workflow_dispatch:  # manual trigger only — removed push trigger

jobs:
  deploy:
    name: SSH deploy (disabled)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "VPS deploy is disabled. Fortress runs on WSL. Deploy manually."

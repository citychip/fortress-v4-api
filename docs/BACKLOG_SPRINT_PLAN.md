# Fortress — Backlog Sprint Plan
**Created 2026-06-19 · Source: HANDOFF optimization backlog (06-18 review) + Catalyst Gate §6 follow-ups + Journal Feedback §6 follow-ups.**
Parapet UI sprints are at 13; these are **system/backend sprints** numbered 15→17 (Sprint 14 = the already-shipped catalyst-gate work).

---


> **Completed Sprints 0–24 (all shipped + verified) → `archive/BACKLOG_COMPLETED.md`.** This file tracks the ACTIVE backlog only.

## Sprint 26 — Risk-manager hardening (from Manus AI Optimalisatievoorstel v5.0, 2026-07-05)
**Source: external strategic/architectural proposal (`Strategisch & Architectonisch Optimalisatievoorstel`, Manus AI, 2026-07-04) — the three items it proposes BEYOND the shipped Sprint 25. All code-complete this session; need deploy + MCP relaunch + Parapet build + a verify pass.**

| # | Item | File(s) | Effort | Status |
|---|---|---|---|---|
| 26.2 | **Full collar (D-03)** — extend `covered_call_candidates` with a **DTE-matched protective put** funded by the covered call. New `_collar_protective_put` (BS put-by-delta, DTE = short-call DTE, ~`collar_put_delta_target` 0.25); each `recommended_call` now carries `protective_put{strike,debit,dte,target_delta}` + `collar_net` (credit − put debit) + `collar_note`. Recovery-page table gains Buy-put / Collar-net columns. | `manage.py` + api.ts + `EfficiencyPage.tsx` + MCP doc | S | ✅ CODE-COMPLETE 2026-07-05 |
| 26.3 | **Manage-at-50% + 21-DTE (D-05 + time-discipline)** — new `GET /api/manage/profit_targets` + MCP `get_profit_targets`: scans open SHORT legs, flags ≥`profit_target_pct` (50%) capture (entry `avg_cost` vs current `market_value`) OR ≤`dte_roll_threshold` (21) DTE → close/roll advisory. Recovery-page **"Manage now"** card. Profit-capture degrades to blank when IBKR avg-cost isn't synced; the 21-DTE flag is always reliable. | `manage.py` + api.ts + `EfficiencyPage.tsx` + MCP | S | ✅ CODE-COMPLETE 2026-07-05 |
| 26.1 | **Health Manager: margin-debt + liquidity monitor (D-06)** — new `GET /api/manage/risk_limits` + MCP `get_risk_limits`: USD-cash margin-debt floor (`margin_debt_limit_usd` −15k) + Excess-Liq floor (25k) + stale-data check, with breach flags (fail-safe: missing → unknown, never "ok"). New **`margin-debt-alert`** scheduled task (weekday 09:03) reads it → 🔴/🟢. Recovery-page **Health Manager banner**. ⏳ Not built: Discord/Slack webhook (optional external wiring), scheduler-health + API-latency monitors, and folding margin awareness into the gateway watchdog. **Verified 2026-07-05:** deployed & callable (`get_risk_limits`); excess-liq monitor works live (🟢 $36.2k > $25k). **`usd_cash` + `data_age_min` are LEDGER-GATED** — confirmed via alias-probe that the fallback positions payload has `net_liq`/`excess_liq` but no cash/sync-time field; they read `null` (fail-safe) until an authenticated ledger sync lands. NOT broken — data-blocked (same root as OAuth Stage 2). Per-leg `avg_cost` IS present, so 26.3 profit-capture works. | `manage.py` + `config_store.py` + api.ts + `EfficiencyPage.tsx` + MCP + scheduled task | M | ✅ DONE 2026-07-05 |

**Deferred from the proposal (not yet scoped):** the watchdog → full "Health Manager" merge (scheduler-health, API-latency, websocket monitors + Discord/Slack heartbeat); documentation migration matrix (Deel III Week 1) if still wanted after the 25.x consolidation.

---

## Sprint 25 — Reliability, visibility & de-concentration (post-Sprint-22 review, 2026-07-04)
**Source: full-stack review after Sprints 21–22 shipped. Three themes, in priority order: (A) make the data backbone trustworthy so the guards stop firing, (B) surface the recovery KPIs that already exist in data but not on screen, (C) turn the concentration *warnings* into an actual glide. Items that already have IDs are ELEVATED here, not duplicated — see the cross-refs.**

### 25.A — Backend reliability (highest leverage)
| # | Item | File(s) | Effort | Dep |
|---|---|---|---|---|
| 25.1 | 🔴 **Gateway reliability / OAuth Stage 2 — the #1 fix.** Almost every recent session fell to `bs_yfinance` with `session_not_established`, which degrades *every* downstream number (roll/stop prices, greeks, liquidity). The Sprint-20/22 guards (SourceBadge, sync-age pill, staleness warnings) treat the symptom. Fix the cause: either finish **OAuth Stage 2** (pending for weeks — see System Status / old Priority 7) or add a **gateway watchdog** (detect 401/`session_not_established` → auto `docker restart cp-gateway` + re-auth, with a cooldown). Removes the recurring 18.8h-stale / delayed-feed traps. | cp-gateway / iBeam + a watchdog task | M–L | — |<br><br>**2026-07-04 OAuth Stage 2 diagnostic (`test_ibkr_oauth.py`):** Still blocked, but the blocker is now isolated to **IBKR's side** — all local causes ruled out. Result: LST is issued, then `POST /iserver/auth/ssodh/init` → `401 :: Invalid signature`. Verified locally: (a) key pair is self-consistent — `public_encryption.pem`/`public_signature.pem` fingerprints match the private-derived ones exactly (enc `727c087adc17f36f813be122828beda831be5f575eb1dec53b1de7f22ba4409c`, sig `fb491dee9bc43238e3bff678ae2f6e696049a6279aaf6029bd1031f5690e6e1f`); (b) `realm="limited_poa"` and full `OAuth1aConfig` are correct; (c) WSL clock in sync (~1s). Since the signature key is accepted for the LST but the LST-signed call is rejected, this is the classic signature of a first-party consumer key (SHARMILAH) IBKR hasn't fully provisioned for a live brokerage session. **Next actions (both IBKR-portal, no repo work):** (1) in the OAuth Self-Service portal, confirm the registered enc/sig public keys match the fingerprints above; (2) if they match, the key isn't activated — open an IBKR API-support ticket citing "LST issues but `/iserver/auth/ssodh/init` returns 401 Invalid signature." Until IBKR activates, the **gateway-watchdog** path is the only one that reduces the `bs_yfinance`/`session_not_established` pain. The atexit `logout()` 401 traceback is harmless (same rejection on shutdown).<br><br>**2026-07-05 — watchdog BUILT, INSTALLED & VALIDATED.** Since OAuth Stage 2 is IBKR-blocked, shipped the gateway-watchdog fallback: `gateway_watchdog.sh` + `fortress-gateway-watchdog.service` (systemd, root for docker access, `Restart=always`, tunables as env). After `WATCHDOG_FAIL_THRESHOLD` consecutive unhealthy probes it does `docker restart cp-gateway` (iBeam auto-re-auths on boot — confirmed live: `IBEAM_ACCOUNT`/`IBEAM_PASSWORD` set, logs "Client login succeeds" → AUTHENTICATED), with a `WATCHDOG_COOLDOWN_SECONDS` guard against restart storms. **Probe correction (same session):** the first cut curled `/v1/api/tickle` and grepped `"authenticated":true` — but on this CP Gateway build a raw hit to `/v1/api/*` is bounced by IBKR's Akamai edge with a "Bad Request" HTML page (verified — not a header/User-Agent issue), which never contains the auth JSON, so it FALSE-negatived a healthy, logged-in gateway and (once) restarted a working container. Fixed: the probe now reads iBeam's **own** truth — `docker logs --since 180s cp-gateway | grep "running and authenticated"` (iBeam's maintenance loop logs it every 60s) — and returns a 3rd "UNKNOWN" state (docker hiccup) that never triggers a restart. Validated: probe returns healthy on the live gateway, watchdog silent (no false restarts). iBeam already self-heals dropped sessions; the watchdog only clears a *wedged* container. This is the lever that reduces the `bs_yfinance` pain regardless of OAuth. |
| 25.2 | ✅ **DONE 2026-07-03 (needs deploy).** Added a 20-min TTL cache to `_fetch_ohlcv` keyed by `ticker|period|interval`; never caches an empty/failed fetch. MTF-panel drill-down repeat requests now hit the cache instead of yfinance. | `chart_route.py` | S | 22.5 ✅ |
| 25.3 | ✅ **DONE 2026-07-03 (needs deploy).** `get_technical_gate` now builds each name's row via `ThreadPoolExecutor` (`_ex.map` preserves order; max 8 workers) — a cold load drops from ~3N sequential yfinance fetches to ~3 wall-clock. Cache writes are atomic under the GIL, so it's safe. | `options_analytics.py` | S | 22.1/22.4a ✅ |
| 25.4 | ✅ **DONE 2026-07-04 (needs deploy).** `get_briefing.macro_regime` now carries `regime_gate` — the canonical synthesized gate (via `get_market_intelligence("SPY")`) — so the briefing and `strategy_metrics` read the same regime. **Timeout guard added 07-04:** the cold-cache QuantData fetch could occasionally stall the briefing past the MCP timeout, so it runs with a hard **2s budget** (ThreadPoolExecutor + `shutdown(wait=False)`) and degrades to `regime_gate=null` rather than blocking the account header. | `briefing.py` | S | 21.3 ✅ |
| 25.5 | ✅ **DONE 2026-07-03 (needs MCP relaunch).** `refresh_iv_data` is now **async by default** — kicks the scan off in a background thread and returns `{status:started}` immediately (poll `get_candidates`/`get_iv_rank` in ~25s); `wait=True` blocks for the inline result. Pure MCP-side change, no backend/out-of-mount edit. | `fortress_mcp_v452.py` | S | — |
| 25.13 | ✅ **DONE 2026-07-04 (needs deploy).** Two-part IV-crush scanner fix so `add_universe_ticker` names reach the Candidates board. **(1) Price-skip:** `get_iv_rank` fetched the yfinance price (`fast_info`) first under one broad try/except → a flaky price call silently skipped tickers whose IV was fine. Reordered: IV/IVR first (the gate), each risky call isolated, price cosmetic (defaults 0, never drops a row). **(2) Report write crashed (the real blocker):** the scanner wrote its report to `~/Fortress_Dashboard/quant` (a dir that doesn't exist) via a `FORTRESS_DATA_DIR` default, so the write `FileNotFoundError`'d and the report never regenerated → the board stayed frozen on a stale 21-row file. Now writes to `_QUANT_DIR` (the scanner's own folder = where `get_candidates` reads). **(3) Parser dropped no-earnings-date rows (the actual last blocker):** `state.parse_crush_report_markdown` did `days = int(re.sub(r"[^0-9\-]","",cols[6]))` — a ticker with no earnings date shows `-`, the regex kept it, `int("-")` raised ValueError, and the `except` dropped the WHOLE row. So JPM/JNJ/MU/CSX (all `-` days, confirmed present in the report file) were silently parsed out. Fixed: strip to digits only → `-` becomes None. **All three layers were needed; the no-earnings-date names hit all three.** | `workflow_05_iv_crush_report.py` + `state.py` | S | 25.9 |

### 25.B — Visibility (surface KPIs already in data)
| # | Item | File(s) | Effort | Dep |
|---|---|---|---|---|
| 25.6 | ✅ **CODE-COMPLETE 2026-07-03 (needs Parapet build + visual check).** Cluster-glide gauge added to the **Recovery** page (the /efficiency page renamed — now hosts both visibility KPIs): current Mag-7 cluster % (from `get_briefing.concentration.cluster`) vs the 60% target with a target marker, gap-to-target, and per-member breakdown, color-coded (>target red → ≤target green). ✅ **Follow-on DONE 2026-07-05 (needs Parapet build + deploy):** the historical glide **line** now ships — new `POST/GET /api/manage/cluster_history` (upsert-by-date store in `cluster_history.json`; ~1 write/day) + MCP `get_cluster_history`; the Recovery page records today's cluster % on load and draws a recharts line vs the ≤60% target. Series builds one point/day going forward (starts sparse — no back-history exists). | `fortress-parapet` + `manage.py` + MCP | S | 19.2 ✅ (23.6) |
| 25.7 | ✅ **CODE-COMPLETE 2026-07-03 (needs Parapet build + visual check).** New `EfficiencyPage.tsx` + `/efficiency` route + sidebar nav (`⊞ Efficiency`). Header stats (portfolio efficiency vs 0.12 benchmark, annualised income, capital-at-risk) + a per-position **log-scaled efficiency bar** ranked desc, color-coded (≥1× green / benchmark–1× yellow / below red), with a **⚠ MONETIZE** flag on positions with real LEAP capital earning < 0.50× (live: AMZN 0.15×, GOOGL 0.25× — feeds the 23.3 call-writing playbook). `CapEffData`/`CapEffRow` types + typed `getCapitalEff` in `api.ts`; page wired into `deploy_parapet.sh` FILES. | `fortress-parapet` | S–M | ✅ (23.1) |
| 25.8 | ✅ **(a)+(b) DONE 2026-07-03 (needs Parapet build).** `TechnicalPage` disposition now surfaces **act-level stops** (`Reduce short leg`) and **warning rolls** (`Roll (warn)`) instead of hiding them behind `Hold`, and splits tactical vs structural wording: `STOP — manage short leg` on a healthy underlying vs `STOP — de-risk now` / `De-risk (weak all TF)` when the thesis is weak. ✅ **(c) CODE-COMPLETE 2026-07-05 (needs Parapet build + visual check).** Candlestick drill-down added to the TechnicalPage row-expand — new `MtfCandleChart.tsx` (recharts floating-bar candle shape; no new charting lib), fed by a new `getChartData` accessor hitting the existing `/api/chart/{ticker}` (22.5). Overlays the weekly 200-SMA Thesis Stop + GEX call/put walls + nearest DP floor as labelled reference lines; lazy-fetched on expand alongside the levels; degrades to "chart unavailable". Wired into `deploy_parapet.sh` FILES. | `fortress-parapet` | S (a,b) / M (c) | 22.4b ✅ |

### 25.C — De-concentration (turn warnings into a glide)
| # | Item | File(s) | Effort | Dep |
|---|---|---|---|---|
| 25.9 | 🟡 **DONE (framework) 2026-07-03.** **(a)** Added **JPM + JNJ** to universe `tier1` (`add_universe_ticker`, screened per 23.4 — financials/healthcare, diversify income out of the 96% Mag-7 cluster). **(b)** Codified the systematic **~0.30Δ / 30–45 DTE LEAP call-writing playbook** as `STRATEGY_ENHANCEMENTS_v3_10.md §8` — monetize the MONETIZE-flagged LEAP cores (GOOGL 0.25× / AMZN 0.15×) via the 21.1b adaptive engine, honoring §4 PMCC guardrails. ✅ **Remaining (full 23.3) DONE 2026-07-05 (needs deploy + MCP relaunch).** New `GET /api/manage/covered_call_candidates` + MCP `get_covered_call_candidates`: scans the per-leg book for LEAP cores (long call, DTE > `leap_core_min_dte` 90) whose contracts aren't fully covered by an open short call on the same underlying (structural "under-written" = held − short calls open), and for each surfaces the adaptive ~0.30Δ / 30–45 DTE covered call sourced straight from the tested `get_strategy_metrics` PMCC leg (short_strike / target_delta / estimated_credit / annualized_yield / pop / delta_rationale) — no re-implemented BS. Advisory-only; earnings-window names get a §4 defer note. ✅ **Follow-on DONE 2026-07-05 (needs Parapet build + deploy):** surfaced on the Recovery page — a `CoveredCalls` card (ticker · unwritten · sell strike/Δ/credit/ann-yield · note) fed by `getCoveredCallCandidates`. | universe + Strategy doc + `manage.py` + MCP + `fortress-parapet` | M | 21.1 ✅ |
| 25.10 | ✅ **DONE 2026-07-03.** Created conditional alert **`f9be085a` — `close_below 382`, critical** (MSFT below the ~$382.58 weekly Thesis Stop → accelerate the LEAP de-risk; daily-close-confirmed, wick-immune per 20.3). MSFT now has a coherent, **fully close-confirmed** 3-alert ladder: `f9be085a` close<382 (Thesis-Stop break → start de-risk), `de612a78` close<375 (deeper break → size the tranche up — replaced the wick-prone `price_below` `8bd4926b`, now deleted), `baa3bc98` close≥395 (trim-into-strength). No more intraday-wick false-fires. ⚠ HANDOFF "Active Conditional Alerts" table references the old `8bd4926b` — refresh next session. | conditional alert | S | 20.3 ✅ |
| 25.11 | ✅ **DONE 2026-07-03 (needs deploy + MCP relaunch)** — closes **19.4b**. New `GET /api/manage/leap_roll_all` + MCP `get_leap_roll_all`: scans the PER-LEG book (the aggregated roll scan skips LEAPs) for long-dated long CALL legs and flags any whose long delta ≤ `leap_roll_delta` (0.70) or DTE ≤ `leap_roll_dte` (120) — the §5/v3.10 §4b roll trigger. Per-leg greeks come from `state.get_active_positions` (they were available all along). Urgency URGENT/WARNING; note the current LEAPs (MSFT/NVDA/GOOGL ~0.74Δ) sit just above the 0.70 trigger. | `manage.py` + MCP | M | ✅ |
| 25.12 | ✅ **DONE 2026-07-03** — closes **24.4**. Created the **`vega-flip-alert`** scheduled task (weekday 15:57 local, after the briefing): reads `get_briefing.greeks.beta_vega_flag` → 🔴 when `net_long_vega` (short-vol book flipped long-vega — an IV spike now hurts), 🟢 when net_short/flat, with a degraded-payload guard (skips on Θ/vega=0 or `bs_yfinance` fallback). Claude-side task, no code. ⚠ User: **Run now** once to pre-approve the Fortress MCP tool. | scheduled task | S | 19.1/20.6 ✅ |

**✅ SPRINT 25 COMPLETE (2026-07-05).** All items 25.1–25.13 shipped, including every follow-on (cluster-glide history line 25.6, covered-call recommender + Recovery panel 25.9/23.3, candlestick drill-down 25.8c). Backend/MCP/Parapet all deployed & committed; the gateway watchdog is live and validated. **Only remaining thread: OAuth Stage 2, which is IBKR-side (consumer-key activation) — nothing to build locally.** Next session: run a fresh full-stack review to scope Sprint 26.

_Historical (original review recommendation): 25.1 (gateway reliability), 25.6 + 25.7 (recovery KPIs visible), 25.10 (MSFT de-risk trigger); 25.2/25.3 cheap backend wins._

---

## Sequencing rationale
- **Sprint 0 first** — without the out-of-mount files, 15.1 / 15.3 / 16.1 / 16.5 can't deploy. One small copy unblocks the two most valuable sprints.
- **Sprint 15 before 16** — `pretrade_check` consolidation (16.1) depends on VIX-term (15.3) and ex-div (15.4) existing as inputs first.
- **16.2 → 16.3 → 16.4** is a strict chain (capture → schema → backfill).
- **Sprint 17** is all independent and deferrable; pull any item forward if a quiet session allows.
- **Sprint 21 before 22/23** — 21.1 (strike-selection fix) is the keystone: it unblocks the LEAP call-writing (23.3) and collar (23.2) work, and 21.4 (trend gate) consumes the weekly SMA that 22.1 ingests. Do **21.1 → 21.2 → 21.6** first (correctness + safety), then 21.4/21.5 (gates), then the UI/structure sprints.
- **22.1 before 21.4** — the trend gate needs the weekly-SMA ingest from the Technical Gate.
- **Sprint 24 trails** its corresponding code sprints (docs after behavior).

## Open decisions
**Resolved 2026-07-02:**
- ✅ 21.1: **adaptive** short-call delta (base 0.30, band 0.20–0.40, 30–45 DTE), engine reasons from IVR/trend/resistance/catalyst/concentration and logs a rationale — see 21.1 design.
- ✅ 21.4: trend gate = **warning** (not a hard block).
- ✅ 22.3: stale/fallback data = **warn clearly**, do not gate write actions.

**Resolved 2026-07-02 (cont.):**
- ✅ 23.2: **full collar** on the concentrated LEAPs (GOOGL/AMZN) — sell an adaptive-Δ call to fund a protective put.

**Resolved 2026-07-02 (cont.):**
- ✅ 23.4: non-tech whitelist screened (Fortress IVR + TradingView weekly trend-gate) across all four sectors. **Add now:** **JPM** (financials, IVR 72, above all weekly MAs) + **JNJ** (healthcare, IVR 90, clean weekly uptrend). **Whitelisted but trend-gated** (add on a weekly-200-SMA reclaim): XOM (energy, marginal), CVX / COST / WMT (currently below their weekly 200-SMA). **Excluded:** UNH (bearish, IVR 23, earnings 14d). LLY optional (rich premium but earnings 34d + high IV). Live demonstration of the 21.4 trend gate: 4 of 8 candidates failed it despite bullish Fortress regime + high IVR.

## Effort key
S = <½ session · M = ~1 session · L = ≳2 sessions (only 22.4 is M–L). Sprints 15–20 shipped. Sprints 21–24 add ~7–9 working sessions; Sprint 21 alone (~2 sessions) captures most of the profit/reliability upside.
